"use strict";

class DomElements{

	constructor(textPop, playerCredits, name, playerLevel, playerMain, playerLeaderBoard){
		this.textPop = textPop;
		this.playerCredits = playerCredits;
		this.name = name;
		this.playerLevel = playerLevel;
		this.playerMain = playerMain;
		this.playerLeaderBoard = playerLeaderBoard;
	}
}